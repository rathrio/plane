function HalfEdge() {
  // Vertex incident_v
  // Face incident_f
  // HalfEdge opposite, next, prev
  // getLength()
  // spanning angle getters: getAlpha, beta
  // Vertex start() {return opposite.incident_v}
  // Vertex end() {return incident_v}
  // hasFace()
  // isOnBorder()
  // toVector() // vector3f pointing from this.start() to this.end()
  // getIncidentAngle()
  // squaredLength() {return this.toVector().lengthSquared()} 
}